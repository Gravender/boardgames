/** Default debounce for match comment autosave (ms). */
export const MATCH_COMMENT_AUTOSAVE_MS = 1500;

/** Default debounce for match metadata text fields (details, etc.) (ms). */
export const MATCH_METADATA_AUTOSAVE_MS = 800;
